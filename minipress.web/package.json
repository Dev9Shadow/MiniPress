{
  "name": "minipress-web",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=public/js/app.js --minify",
    "dev":   "esbuild src/main.ts --bundle --outfile=public/js/app.js --watch"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
